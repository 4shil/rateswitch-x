# Memory — 2026-02-28

## Skills Installed (ClawHub)
- ui-ux-design, ui-audit, frontend-design-extractor (force), backend, fullstack-developer
- react-native, native-ui, react-expert, frontend-dev, react-best-practices, react-performance
- firebase-auth-setup, shadcn-ui, remotion, remotion-video-toolkit, realtime-react-hooks
- react, vercel-react-native-skills, copilotkit-react
- self-improving, self-improving-agent, humanize-ai-text
- expo-native-ui — scanned, NOT installed (VirusTotal flagged, static scan inconclusive)
- react-native-update, react-nextjs-generator — flagged, NOT installed

## GitHub READMEs Updated (4shil repos)
- Atlas, LearnPD, MemoryLink-AR, DeonAi, deonai-cli — fully rewritten (no emoji, developer style, one GIF each)
- rateswitch-x, Musicya, Giffy, Axium-TempFiles, Qotizs, Qraw-QR, Password-Manager, Fenster, Qubyts, Nebula-Mini, TeaAi — already had clean READMEs, left as-is
- Style guide: no emojis, no banner GIFs, no badges (except live demo links), ruvector-style technical docs
- AiraBot, kinemouse — already good, skipped

## Storage / Partition Work
- nvme0n1p7 (170GB ext4) successfully mounted as /home
- fstab entry added: UUID=e03f1bc3-09a8-4357-90ac-1c6669bdc155 /home ext4 defaults 0 2
- Windows partitions: nvme0n1p3 (UUID: 01DC3343E96EF260), nvme0n1p4 (UUID: 4A9A84B29A849C57)
- User wants to hide Windows partitions from file manager via udev rules (instructions given, not yet confirmed done)

## OpenClaw GitHub
- Filed MCP Client feature request: github.com/openclaw/openclaw/issues/29053
- Upvoted as @4shil

## Hyprland
- Ashil wants to customize Wayland/Hyprland — topic opened at end of session, not started yet
- Config location: ~/.config/hypr/UserConfigs/UserKeybinds.conf
- Waybar toggle: Super+Ctrl+B

## Tech Discussions
- MCP (Model Context Protocol) — Anthropic, open source MIT, tool calling standard
- ACP (Agent Communication Protocol) — IBM/Linux Foundation, agent-to-agent protocol, now merged into A2A
- entire.io — new developer platform, GitHub rebuilt for AI agent era, checkpoints = commit + agent session
- LocalSend recommended for file transfer over hotspot

## Hyprland / Wayland Customization (session 2)
- Full liquid glass theme applied (Apple iOS 26 / macOS Tahoe inspired)
- UserDecorations.conf: blur passes=4, size=10, vibrancy=0.20, rounding=20, triple gradient border
- UserAnimations.conf: popin 85% animations, smooth bezier curves, vertical workspace slide
- Waybar style.css: full rewrite — frosted glass pill containers, rgba white borders, liquid glass active workspace pill, pastel translucent module colors. Backup at ~/.config/waybar/style.css.bak
- foot terminal: alpha=0.50, background=080810 (near black with blue undertone), Apple system colors
- foot errors fixed: dim-inactive removed, page_up→Prior, page_down→Next, duplicate [colors] section cleaned
- Super+T fixed: binary is `Telegram` (capital T), not telegram-desktop
- Super+B changed: brave → google-chrome-stable

## Reminders / Pending
- Storage migration steps given but not confirmed complete — home rsync + fstab done (nvme0n1p7 shows as /home in lsblk)
- Windows hide from file manager — udev rules instructions given, needs terminal run by Ashil
- Hyprland customization — pending, Ashil wants to start

---

## Atlas App — Production Build Session (afternoon)

### Phase 1 — Bug Fixes & Stabilization ✅
- ErrorBoundary component (`src/components/ErrorBoundary.tsx`) — wraps ThemeProvider in _layout.tsx
- Dark mode NativeWind fix — ScreenWrapper now reads `useTheme().isDark`, applies `dark` class to root SafeAreaView, StatusBar dynamic
- Notifications screen created (`src/app/notifications.tsx`) — overdue/upcoming/scheduled sections
- Gallery notifications button fixed (was → /settings, now → /notifications)
- Map real user location via expo-location + reverseGeocodeAsync
- Live weather via wttr.in (free, no API key)
- Past date validation in add-goal.tsx (Alert on past date)
- Image compression quality 0.6 (was 1.0)
- Prettier config added (.prettierrc)

### Phase 2 — Infrastructure ✅
- Jest + jest-expo + @testing-library/react-native installed
- Tests: `__tests__/store/useGoalStore.test.ts` (7 tests), `__tests__/utils/dateUtils.test.ts`
- GitHub Actions CI: `.github/workflows/ci.yml` — tsc + jest on push/PR
- EAS config: `eas.json` (development/preview/production profiles)

### Phase 3 — Backend ✅
- Supabase project: https://cjkdgktttnawsmbptszy.supabase.co
- Anon key stored in `.env` (gitignored)
- `src/lib/supabase.ts` — singleton client with AsyncStorage session
- `src/lib/database.types.ts` — full typed schema
- `supabase/schema.sql` — profiles + goals tables, RLS policies, storage bucket, triggers
- Schema was run successfully in Supabase SQL editor
- `src/store/useAuthStore.ts` — session management, signIn/signUp/signOut/resetPassword
- `src/app/auth.tsx` — sign in / sign up / forgot password screen with Toast feedback
- Auth gate in `index.tsx` — no session → /auth, no onboard → /onboarding, else → /(tabs)
- `useGoalStore` rewritten — offline-first (AsyncStorage) + Supabase cloud sync on all CRUD ops
- `useProfileStore` rewritten — same pattern, syncs name/bio/avatar/hasOnboarded
- Image upload to Supabase Storage (`goal-images` bucket) on add/edit goal
- Sign out button added to settings.tsx

### Phase 4 — Polish ✅
- Sentry: `src/lib/sentry.ts` — initialize via EXPO_PUBLIC_SENTRY_DSN in .env
- Husky pre-commit: `npx lint-staged` (Prettier + ESLint on staged files)
- `src/theme/colors.ts` — semantic color utilities + `semanticClasses` object
- `src/components/Skeleton.tsx` — GoalCardSkeleton, GoalListSkeleton, ProfileHeaderSkeleton
- `src/components/Toast.tsx` + `useToast()` hook
- `src/components/LoadingScreen.tsx` — animated dots loading screen, used in index.tsx
- `src/components/EmptyState.tsx` — used in gallery + archive
- Pull-to-refresh on home tab (calls syncFromCloud)
- Light/dark semantic classes applied across all screens
- Unit system wired: `src/utils/unitUtils.ts` (formatDistance/Temperature/Elevation), map weather uses it
- README rewritten as full app documentation

### Design Thinking Session ✅
- `src/components/NextDreamHero.tsx` — closest upcoming goal as full-bleed hero card, progress bar, momentum stats row
- `src/components/HeroEmpty` — motivational empty state ("Every great journey starts with a dream")
- `src/components/GoalRow.tsx` — replaces SwipeableGoalRow, thumbnail + countdown badge + complete toggle
- `src/components/Confetti.tsx` — 60-particle confetti burst on goal completion (Peak-End Rule)
- `src/components/Confetti` wired into goal-detail.tsx celebration overlay
- Motivational "Keep Going" block in goal-detail (contextual copy based on days left)
- Dead code deleted: DashboardOverview.tsx, ProgressRing.tsx, SwipeableGoalRow.tsx
- FAB (blue + button) injected into CustomTabBar between gallery and map tabs
- Loss Aversion: overdue GoalRow gets red-tinted background
- Goal Gradient Effect: "X dreams ahead · Y lived · Z%" stats row in NextDreamHero
- Endowment framing in home screen list header ("X dreams ahead" vs "Upcoming Adventures")
- Home screen computes nextGoal + overdueGoals once (no double sort)
- Gallery redundant `handleNext = goNext` alias removed

### Key Files
- Atlas location: ~/Coding/Atlas/
- Roadmap: ~/Coding/Atlas/ATLAS_ROADMAP.md
- Supabase URL: https://cjkdgktttnawsmbptszy.supabase.co (in .env, not committed)
- GitHub: github.com/4shil/Atlas (main branch)
- App bundle ID: com.atlas.bucketlist

### Commit Style Preference
- No prefix (no "feat:", "fix:", "docs:" — just plain descriptive message)
- Commit only when feature/fix is complete and coherent — never mid-feature
