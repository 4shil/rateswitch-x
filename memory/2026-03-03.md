# Memory — 2026-03-03 (Tuesday)

## Atlas App Analysis & Improvements

- **App**: Atlas — React Native / Expo travel goals app at `~/Coding/Atlas`
- **Stack**: Expo 53, React Native 0.79, Expo Router, Zustand + AsyncStorage, Supabase (auth + DB + storage), NativeWind, Reanimated, Sentry
- **Supabase**: fully wired — auth screens, goals table, storage, offline sync, RLS

### Known Issues Found
- Onboarding gate broken — `index.tsx` hardcodes redirect to tabs, ignores `hasOnboarded`
- Notification ID mismatch — `Date.now()` used instead of goal's UUID
- "Delete Account" only clears local data — doesn't touch Supabase
- 4 dead buttons on map screen (back, filter, recenter, "see all")
- Dead header buttons: archive (menu, settings), gallery (notifications bell)
- Terms of Service + Privacy Policy are plain text, not tappable
- Map filter pills don't update map pins
- `completionPhoto` referenced in goal-detail.tsx but not wired up
- `@shopify/react-native-skia v2.0.0-next.4` — pre-release, unstable
- `react-native-worklets-core` duplicated in deps + devDeps
- Sync conflict resolution: cloud always wins, no `updated_at` comparison

### 24-Improvement Plan + Sub-Agent
- Compiled 24 improvements covering: bugs, UX, missing features, performance, App Store readiness
- Spawned sub-agent (session `agent:main:subagent:18d00f29-57e9-47b1-86c3-20fb19d3065e`, runId `55cf613b-dc9a-4991-90af-bcc47aad1888`) to implement all 24
- Each improvement = 1 commit, push after every commit to `github.com/4shil/Atlas` (branch `main`)
- Sub-agent still running as of 11:15 PM IST — auto-announces on completion

### Key files
- `src/app/index.tsx` — onboarding gate fix target
- `src/app/(tabs)/map.tsx` — dead buttons + filter + clustering
- `src/app/settings.tsx` — delete account, legal links
- `src/store/useGoalStore.ts` — milestones, pagination, sync conflict fix
- `supabase/schema.sql` — needs milestones JSONB, completion_photo_url columns
- `src/components/GoalCard.tsx`, `GoalRow.tsx` — memoization + CDN transforms
